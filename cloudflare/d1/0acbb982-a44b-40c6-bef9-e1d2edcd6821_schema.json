{
  "tables": [
    "users",
    "sqlite_sequence",
    "works",
    "work_snapshots",
    "cloud_api_keys",
    "sync_logs",
    "password_reset_codes"
  ],
  "schemas": [
    "CREATE TABLE _cf_KV (\n        key TEXT PRIMARY KEY,\n        value BLOB\n      ) WITHOUT ROWID",
    "CREATE TABLE users (\n  id INTEGER PRIMARY KEY AUTOINCREMENT,\n  email TEXT NOT NULL UNIQUE,\n  password_hash TEXT NOT NULL,\n  nickname TEXT DEFAULT '',\n  avatar TEXT DEFAULT '',\n  visit_count INTEGER DEFAULT 0,\n  last_visit_at TEXT,\n  created_at TEXT DEFAULT (datetime('now')),\n  updated_at TEXT DEFAULT (datetime('now'))\n)",
    "CREATE TABLE sqlite_sequence(name,seq)",
    "CREATE TABLE works (\n  id INTEGER PRIMARY KEY AUTOINCREMENT,\n  user_id INTEGER NOT NULL,\n  work_id TEXT NOT NULL,\n  title TEXT DEFAULT '',\n  category TEXT DEFAULT '',\n  synopsis TEXT DEFAULT '',\n  payload TEXT DEFAULT '{}',\n  version INTEGER DEFAULT 1,\n  chapter_count INTEGER DEFAULT 0,\n  total_words INTEGER DEFAULT 0,\n  created_at TEXT DEFAULT (datetime('now')),\n  updated_at TEXT DEFAULT (datetime('now'))\n)",
    "CREATE TABLE work_snapshots (\n  id INTEGER PRIMARY KEY AUTOINCREMENT,\n  work_id INTEGER NOT NULL,\n  payload_hash TEXT DEFAULT '',\n  payload TEXT DEFAULT '{}',\n  version INTEGER DEFAULT 1,\n  size INTEGER DEFAULT 0,\n  created_at TEXT DEFAULT (datetime('now'))\n)",
    "CREATE TABLE cloud_api_keys (\n  id INTEGER PRIMARY KEY AUTOINCREMENT,\n  user_id INTEGER NOT NULL,\n  provider TEXT NOT NULL,\n  key_hash TEXT NOT NULL,\n  last4 TEXT DEFAULT '',\n  created_at TEXT DEFAULT (datetime('now'))\n)",
    "CREATE TABLE sync_logs (\n  id INTEGER PRIMARY KEY AUTOINCREMENT,\n  user_id INTEGER NOT NULL,\n  work_id TEXT NOT NULL,\n  action TEXT DEFAULT 'sync',\n  device TEXT DEFAULT '',\n  ip TEXT DEFAULT '',\n  created_at TEXT DEFAULT (datetime('now'))\n)",
    "CREATE TABLE password_reset_codes (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, email TEXT NOT NULL, code TEXT NOT NULL, expires_at TEXT NOT NULL, used INTEGER DEFAULT 0, created_at TEXT DEFAULT (datetime('now')))"
  ]
}