// architecture.js - 占位脚本
// 注：本文件之前误填充为 HTML 页面内容，现更正为 JS。
// 架构设计核心逻辑已经内联在 architecture.html 底部的 <script> 中，
// 包括四级导航（goToLevel）、模块切换（switchArchModule）、
// AI 生成主流程（generateArchModule）等全部核心逻辑。
// 本文件保留为占位，避免 HTML 中对该文件的 script 引用失效。
if (!window.__architecture_js_loaded) {
  window.__architecture_js_loaded = true;
}
