// ==========================================================================
// engines-memory-skill// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.Memory// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  v// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(tex// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length &&// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]]// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j =// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = tru// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union +=// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] ||// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m =// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) {// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0;// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb -// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLong// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedg// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text ||// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core',// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k]// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text ||// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jacc// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = j// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasC// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effec// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasC// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm =// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (l// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.ch// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s) { return s.text.length > 10; });
      if (summaries.length// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s) { return s.text.length > 10; });
      if (summaries.length < 5) return [];
      var patternTemplates = [
        { name: '升级打怪// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s) { return s.text.length > 10; });
      if (summaries.length < 5) return [];
      var patternTemplates = [
        { name: '升级打怪', keywords: ['突破', '晋升', '进阶', '击败', '战胜', '// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s) { return s.text.length > 10; });
      if (summaries.length < 5) return [];
      var patternTemplates = [
        { name: '升级打怪', keywords: ['突破', '晋升', '进阶', '击败', '战胜', '修炼', '提升', '实力', '境界'], minMatches: 3 },
        { n// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s) { return s.text.length > 10; });
      if (summaries.length < 5) return [];
      var patternTemplates = [
        { name: '升级打怪', keywords: ['突破', '晋升', '进阶', '击败', '战胜', '修炼', '提升', '实力', '境界'], minMatches: 3 },
        { name: '寻宝奇遇', keywords: ['发现', '找到', '获得',// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s) { return s.text.length > 10; });
      if (summaries.length < 5) return [];
      var patternTemplates = [
        { name: '升级打怪', keywords: ['突破', '晋升', '进阶', '击败', '战胜', '修炼', '提升', '实力', '境界'], minMatches: 3 },
        { name: '寻宝奇遇', keywords: ['发现', '找到', '获得', '得到', '奇遇', '机缘', '传承', '宝物', '秘境', '// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s) { return s.text.length > 10; });
      if (summaries.length < 5) return [];
      var patternTemplates = [
        { name: '升级打怪', keywords: ['突破', '晋升', '进阶', '击败', '战胜', '修炼', '提升', '实力', '境界'], minMatches: 3 },
        { name: '寻宝奇遇', keywords: ['发现', '找到', '获得', '得到', '奇遇', '机缘', '传承', '宝物', '秘境', '遗迹'], minMatches: 3 },
        { name: '复仇归来', keywords: ['// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s) { return s.text.length > 10; });
      if (summaries.length < 5) return [];
      var patternTemplates = [
        { name: '升级打怪', keywords: ['突破', '晋升', '进阶', '击败', '战胜', '修炼', '提升', '实力', '境界'], minMatches: 3 },
        { name: '寻宝奇遇', keywords: ['发现', '找到', '获得', '得到', '奇遇', '机缘', '传承', '宝物', '秘境', '遗迹'], minMatches: 3 },
        { name: '复仇归来', keywords: ['复仇', '报仇', '雪恨', '归来', '回归', '复仇者', '// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s) { return s.text.length > 10; });
      if (summaries.length < 5) return [];
      var patternTemplates = [
        { name: '升级打怪', keywords: ['突破', '晋升', '进阶', '击败', '战胜', '修炼', '提升', '实力', '境界'], minMatches: 3 },
        { name: '寻宝奇遇', keywords: ['发现', '找到', '获得', '得到', '奇遇', '机缘', '传承', '宝物', '秘境', '遗迹'], minMatches: 3 },
        { name: '复仇归来', keywords: ['复仇', '报仇', '雪恨', '归来', '回归', '复仇者', '仇恨', '血债'], minMatches: 2 },
        { name: '身份揭晓// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s) { return s.text.length > 10; });
      if (summaries.length < 5) return [];
      var patternTemplates = [
        { name: '升级打怪', keywords: ['突破', '晋升', '进阶', '击败', '战胜', '修炼', '提升', '实力', '境界'], minMatches: 3 },
        { name: '寻宝奇遇', keywords: ['发现', '找到', '获得', '得到', '奇遇', '机缘', '传承', '宝物', '秘境', '遗迹'], minMatches: 3 },
        { name: '复仇归来', keywords: ['复仇', '报仇', '雪恨', '归来', '回归', '复仇者', '仇恨', '血债'], minMatches: 2 },
        { name: '身份揭晓', keywords: ['真相', '身份', '原来', '竟然是', '竟是', '居然是// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s) { return s.text.length > 10; });
      if (summaries.length < 5) return [];
      var patternTemplates = [
        { name: '升级打怪', keywords: ['突破', '晋升', '进阶', '击败', '战胜', '修炼', '提升', '实力', '境界'], minMatches: 3 },
        { name: '寻宝奇遇', keywords: ['发现', '找到', '获得', '得到', '奇遇', '机缘', '传承', '宝物', '秘境', '遗迹'], minMatches: 3 },
        { name: '复仇归来', keywords: ['复仇', '报仇', '雪恨', '归来', '回归', '复仇者', '仇恨', '血债'], minMatches: 2 },
        { name: '身份揭晓', keywords: ['真相', '身份', '原来', '竟然是', '竟是', '居然是', '身世', '秘密', '揭晓', '暴露'], minMatches// ==========================================================================
// engines-memory-skill/core.js — PART 0: 基础工具层 + PART 1: 记忆推理引擎
// ==========================================================================
;(function () {
  'use strict';
  var parts = window.MemorySkillParts = window.MemorySkillParts || {};

  // ---- 工具函数 ----
  var STOPWORDS = {};
  (function () {
    var sw = '的 了 是 在 我 你 他 她 它 我们 你们 他们 它们 这个 那个 这些 那些 一 一个 一些 这 那 和 与 及 或 但 但是 然而 不过 因为 所以 因此 于是 然后 接着 之后 之前 现在 过去 将来 已经 正在 会 能 能够 可以 可 就 就是 只是 只有 就是 一下 一直 依然 依旧 很 非常 十分 极其 太 最 更 比较 稍微 几乎 差不多 大概 大约 或许 也许 可能 应该 必须 需要 想 要 来 去 走 跑 看 听 说 做 把 被 将 给 向 往 于 为 对 以 用 从 到 在 其 之 而 所 以 为 因 所以 因此 于是 然后 接着 同时 但是 然而 不过 只是 只有 就是 而且 并且 还 也 都 全 皆 尽 仅 只 光 却 倒 反 反而 尽管 虽然 既然 那么 这样 那样 如何 什么 怎么 为什么 哪里 哪个 谁 多少 几 若 如果 假如 假设 要是 否则 不然 又 再 才 只 都 也 就是 正是 真是 非常 极其 十分 很 太 更 最 好 坏 新 旧 老 大 小 高 矮 长 短 远 近 快 慢 多 少 早 晚 先 后 前 里 外 上 下 左 右 中 内 东 南 西 北 是 否 有 无 没 没有 不 不是 不要 别 非 未 莫 勿 啊 呀 哦 哈 哎 嗯 唉 喔 咦 嘿 嘻 呢 吧 吗 嘛';
    sw.split(/\s+/).forEach(function (w) { STOPWORDS[w] = true; });
  })();

  function tokenize(text) {
    if (!text) return [];
    text = String(text).replace(/[\s\u3000]+/g, ' ');
    var segs = text.split(/[，。！？！？：；、,.!?;:\s]+/).filter(Boolean);
    var tokens = [];
    for (var i = 0; i < segs.length; i++) {
      var s = segs[i];
      for (var j = 0; j < s.length; j++) {
        if (/[a-zA-Z0-9]/.test(s[j])) {
          var k = j;
          while (k < s.length && /[a-zA-Z0-9]/.test(s[k])) k++;
          tokens.push(s.substring(j, k));
          j = k - 1;
        } else {
          if (!STOPWORDS[s[j]]) tokens.push(s[j]);
          if (j + 1 < s.length) {
            var bg = s.substring(j, j + 2);
            if (!STOPWORDS[bg]) tokens.push(bg);
          }
        }
      }
    }
    return tokens;
  }

  function jaccard(a, b) {
    if (!a || !b) return 0;
    var sa = {}, sb = {};
    for (var i = 0; i < a.length; i++) sa[a[i]] = (sa[a[i]] || 0) + 1;
    for (var j = 0; j < b.length; j++) sb[b[j]] = (sb[b[j]] || 0) + 1;
    var inter = 0, union = 0;
    var all = {};
    for (var k in sa) all[k] = true;
    for (var kk in sb) all[kk] = true;
    for (var kkk in all) {
      inter += Math.min(sa[kkk] || 0, sb[kkk] || 0);
      union += Math.max(sa[kkk] || 0, sb[kkk] || 0);
    }
    return union > 0 ? inter / union : 0;
  }

  function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    var m = [];
    for (var i = 0; i <= b.length; i++) { m[i] = [i]; }
    for (var j = 0; j <= a.length; j++) { m[0][j] = j; }
    for (var ib = 1; ib <= b.length; ib++) {
      for (var jb = 1; jb <= a.length; jb++) {
        m[ib][jb] = b.charAt(ib - 1) === a.charAt(jb - 1) ? m[ib - 1][jb - 1] : Math.min(m[ib - 1][jb - 1], m[ib][jb - 1], m[ib - 1][jb]) + 1;
      }
    }
    return m[b.length][a.length];
  }

  function getLM(work) {
    if (!work) return null;
    if (!work.longMemory) {
      if (typeof initLongMemory === 'function') initLongMemory(work);
      else work.longMemory = {};
    }
    return work.longMemory;
  }

  // ---- PART 1：记忆推理引擎 ----
  var Reasoning = {
    inferCausality: function (work, eventA, eventB) {
      var lm = getLM(work);
      if (!lm) return null;
      var allEvents = [];
      (lm.chapterIndex || []).forEach(function (ci) {
        allEvents.push({ text: ci.summary || '', chapter: ci.chapterIdx || 0, source: 'chapterIndex' });
      });
      (lm.timelineEvents || []).forEach(function (te) {
        allEvents.push({ text: te.text || '', chapter: te.chapterIdx || 0, source: 'timeline' });
      });
      (lm.foreshadowLedger || []).forEach(function (fl) {
        allEvents.push({ text: fl.text || '', chapter: fl.chapterIdx || 0, source: 'foreshadow' });
      });
      var anchors = lm.memoryAnchors || {};
      ['core', 'promises', 'hooks'].forEach(function (k) {
        (anchors[k] || []).forEach(function (a) {
          allEvents.push({ text: a.text || '', chapter: a.chapterIdx || 0, source: 'anchor_' + k });
        });
      });
      var causeWords = ['因为', '由于', '导致', '造成', '引发', '触发', '使得', '让', '迫使', '逼得', '害得', '引起'];
      var effectWords = ['所以', '因此', '于是', '结果', '最终', '终于', '终于', '这才', '从而', '进而'];
      var tokensA = tokenize(eventA);
      var tokensB = tokenize(eventB);
      var chain = [];
      allEvents.sort(function (a, b) { return a.chapter - b.chapter; });
      for (var i = 0; i < allEvents.length; i++) {
        var simA = jaccard(tokensA, tokenize(allEvents[i].text));
        var simB = jaccard(tokensB, tokenize(allEvents[i].text));
        if (simA > 0.2 || simB > 0.2) {
          chain.push({
            event: allEvents[i].text.substring(0, 80),
            chapter: allEvents[i].chapter,
            similarityToA: simA,
            similarityToB: simB,
            source: allEvents[i].source
          });
        }
      }
      var hasCausalLink = chain.some(function (c) {
        return causeWords.some(function (w) { return c.event.indexOf(w) >= 0; }) ||
          effectWords.some(function (w) { return c.event.indexOf(w) >= 0; });
      });
      return {
        chain: chain,
        hasCausalLink: hasCausalLink,
        confidence: hasCausalLink ? 0.7 : chain.length > 1 ? 0.4 : 0.1,
        intermediateEvents: chain.length
      };
    },

    detectPatterns: function (work) {
      var lm = getLM(work);
      if (!lm) return [];
      var summaries = (lm.chapterIndex || []).map(function (ci) {
        return { chapter: ci.chapterIdx || 0, text: ci.summary || '' };
      }).filter(function (s) { return s.text.length > 10; });
      if (summaries.length < 5) return [];
      var patternTemplates = [
        { name: '升级打怪', keywords: ['突破', '晋升', '进阶', '击败', '战胜', '修炼', '提升', '实力', '境界'], minMatches: 3 },
        { name: '寻宝奇遇', keywords: ['发现', '找到', '获得', '得到', '奇遇', '机缘', '传承', '宝物', '秘境', '遗迹'], minMatches: 3 },
        { name: '复仇归来', keywords: ['复仇', '报仇', '雪恨', '归来', '回归', '复仇者', '仇恨', '血债'], minMatches: 2 },
        { name: '身份揭晓', keywords: ['真相', '身份', '原来', '竟然是', '竟是', '居然是', '身世', '秘密', '揭晓', '暴露'], minMatches: 2 },
        { name: '英雄救美', keywords: ['救下', '